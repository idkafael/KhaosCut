# Arquivo de exemplo para produção
# Copie este arquivo para .env.production.local ou configure no servidor

# Next.js define NODE_ENV automaticamente:
# - npm run dev → NODE_ENV=development
# - npm run build → NODE_ENV=production  
# - npm start → NODE_ENV=production

# Para forçar produção, você pode definir aqui:
NODE_ENV=production

# Outras variáveis de ambiente podem ser adicionadas aqui
